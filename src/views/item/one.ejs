<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Item <%= item.item_id %></title>
</head>
<body>
    <%- include('../nav') %>
    <main>
        <h2>Item <%= item.item_id %> Page</h2>
        <h3><%= item.item_name %></h3>
        <p>Stock: <%= item.stock %></p>
        <p>Category: <a href=<%- `/category/${item.cat_id}` %>><%= item.cat_name %></a></p>
        <p>Brand: <a href=<%- `/brand/${item.brand_id}` %>><%= item.brand_name %></a></p>
        <form action=<%- `/item/${item.item_id}/delete` %> method="post"><button type="submit">Delete</button></form>
        <a href=<%- `/item/${item.item_id}/edit` %>>Edit</a>
    </main>
</body>
</html>